spring:
  application:
    name: sandbox
  profiles:
    active: h2          # "test" is used by the test suite; there is no other profile
  jpa:
    # Off deliberately: entities must be mapped to DTOs inside the service transaction. Leaving it
    # on lets lazy loads fire during JSON serialization, which is how N+1 queries sneak in.
    open-in-view: false

management:
  endpoints:
    web:
      exposure:
        include: health,info

springdoc:
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.templateai: DEBUG

---
# ---- h2: the default. In-memory, seeded on boot, rebuilt on every restart. ----
# MODE=PostgreSQL so the SQL and JPA you write here maps onto a real Postgres later without
# rewrites. show-sql is on: it is the only way to confirm the query you think you wrote is the
# query that ran.
spring:
  config:
    activate:
      on-profile: h2
  datasource:
    url: jdbc:h2:mem:sandbox;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console

---
# ---- test: isolated database, quiet logs, no demo seed. ----------------------
spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
logging:
  level:
    com.templateai: INFO
    org.hibernate: WARN
